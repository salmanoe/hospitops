package id.co.hospitops.channel.application.command;

/** Create or update the current hotel's provider property hookup. */
public record ConfigureChannelPropertyCommand(String externalPropertyId) {
}
